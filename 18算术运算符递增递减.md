-- ++

```cpp
int a = 10;
++a; // a = 11

int b = 10;
b++; // b = 11

// 前置递增： 先让变量+1，在进行表达式的运算
// 后置递增：先进性表达式的运算，再让变量+1
int a = 10;
int b = ++a * 10; // b = 110 todo: a = ++a * 10; a=?
int b = a++ * 10; // b = 100 todo: a = a-- * 10; a=?

```